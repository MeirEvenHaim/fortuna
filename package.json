{
  "name": "aba_ba_root",
  "private": true,
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "start-program": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
